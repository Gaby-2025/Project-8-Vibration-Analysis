We analyze motor vibration using an accelerometer. The data is then processed in Arduino and converted into plots through the serial monitor.
To analyze the frequency content of the signal we process the data in python and produce two plots. These plots show motor performance and peak frequency.
